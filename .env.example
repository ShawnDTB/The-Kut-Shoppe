# Optional, disposable browser-only workflow for design review under npm run dev.
# Ignored in production builds. Never enter real customer data in this mode.
VITE_LOCAL_PLATFORM_PREVIEW=false
# Browser variables are public. Never put account passwords or API secrets here.
